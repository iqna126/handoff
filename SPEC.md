# 产品行为规格

> 记录**应用做什么**。架构与技术选型见 `DESIGN.md`，实现手法见代码与测试。
>
> **收录标准**：换个人实现会不会做出不一样的东西？会 → 记在这里。
> 纯实现手法差异（用哪个函数、循环怎么写）不收录。
>
> 本文档记录的行为均已在现有版本中实现并验证，重构时**必须保持**，除非明确决定改变。

---

## 0. 全局约定

| 项 | 规则 | 原因 |
|---|---|---|
| 术语 | 见 §0.1 | 用词错了用户会当场出戏 |
| 重量存储 | **一律存 kg**，见 §0.2 | 切换单位不累积舍入误差 |
| 日期 | `YYYY-MM-DD` 字符串 | 跨时区不歧义 |
| 一周起始 | **周一** | 用户要求；国内习惯 |
| 数字字体 | 等宽 | 重量/组次要能竖向对齐扫读 |
| 破坏性操作 | 一律弹窗二次确认 | 删除、清空 |

### 0.2 单位换算

三条必须遵守，否则会出现「输入 180 lb 变成 179.9」这类漂移：

1. **系数用精确值 `1 lb = 0.45359237 kg`**，不要用 `0.4536` 这类近似值
2. **内部一律保留完整精度，只在显示这一步处理**。
   存储时就舍入会导致 `180lb → 81.6kg → 179.9lb`
2b. **显示不得改写用户的输入。** 用户输 `125.25`，就必须显示 `125.25`，
   不能舍成 `125.3`。做法：抹掉浮点噪声后最多保留 2 位小数、去掉多余的 0，
   而不是强制舍到固定位数
3. **切换单位时从存储的 kg 重新换算，不要拿界面上显示的（已舍入的）数去反推**。
   反推会让每切换一次就掉一点精度，切几次能差出一两公斤

验证方式：
- 输入 180 lb，反复切换单位 10 次，应始终显示 180 lb
- 输入 125.25 lb，各处显示均应为 125.25，不得变成 125.3 或 125.2
- 从设置页切换单位时，计算器的数值必须真正换算，不能把 lb 的数字直接当 kg 显示

### 0.3 配重取整

百分比配重必须换算成**实际能配出来的重量**。算出 94.7 lb 是没用的，那个重量配不出来。

**器材（可配置，默认按主用户的 box）**

| 单位 | 杠铃杆 | 片 |
|---|---|---|
| lb | 45（男）/ 35（女） | 45, 25, 15, 10, 5, 2.5, 1.25 |
| kg | 20（男）/ 15（女） | 25, 20, 15, 10, 5, 2.5, 1.25 |

**规则**

- 最小增量 = 最小片 × 2（两边各挂一片）。lb 套是 2.5 lb，kg 套是 2.5 kg
- 取**最接近**的可配重量，不是向下取整
- 低于杆重时直接返回杆重
- 同时显示精确值（小字），取整后与精确值一致时不显示
- 点某一档要能看到**每边挂哪些片**（如 `45lb 杆 ＋ 每边 25 + 15 + 2.5`）
- 杆重选择要持久化，且 lb / kg 各记一个

**配片算法**：片从大到小贪心。上表的片值贪心即最优解。

### 0.1 术语表

面向用户的文案统一用下列词，**不要自创或直译英文**：

| 概念 | 用 | 不要用 | 说明 |
|---|---|---|---|
| 课表给定的重量/组次 | **计划** | ~~处方~~ | 「处方」是医疗用语，健身语境不这么说 |
| Quadriceps | **股四头** | ~~腿前~~ | 正规解剖名 |
| Hamstrings | **腘绳肌** | ~~腿后~~ | 正规解剖名 |
| Glutes | 臀 | | |
| Calves | 小腿 | | |
| Core | 核心 | | |

**动作名一律用 CrossFit 官方英文命名，不翻译成中文。**（`Back Squat` 不写「后蹲」）
肌群名用中文，因为它是标签不是专有名词。

---

## 1. 今日

### 1.1 日历

- **周一开始**，表头 `一 二 三 四 五 六 日`
- 支持 **周视图 / 月视图** 切换，切换按钮在标题右侧
- 周视图显示所选日期所在的那一周（7 格）；月视图显示整月并补齐首尾成完整周
- 月视图有上/下月导航
- **等宽 7 列网格**，左右边缘与下方卡片对齐

**三种状态互相独立**：

| 状态 | 表现 | 说明 |
|---|---|---|
| 今天 | 实心主色块 | 无论是否被选中都显示 |
| 选中 | 描边 | 用户当前查看的日期 |
| 今天且选中 | 实心 + 深色描边 | 两者叠加 |

> 今天和选中必须分开表示。用户翻到其他日期时仍要能一眼看到今天是哪天。

- 有未完成待办或训练记录的日期，下方显示小圆点
- 点击日期 → 切换查看该日的待办与训练

### 1.2 内容区

- 待办：显示所选日期的待办，可勾选完成
- 训练：显示所选日期的训练记录（标题 + 动作行 + 肌群标签），**纯展示，不可点击跳转**
- 想法：快速输入框，有内容时才出现保存按钮

> 训练区不做跳转 —— 用户认为多余。

---

## 2. 待办

- 输入框 + 日期按钮，**按 Enter 添加**
- 日期按钮显示「今天」或 `MM-DD`
- **自定义日期选择器**（周一开始）。不使用原生 `<input type="date">`，因为其一周起始日由系统语言决定，无法通过代码控制
- 选择器内含「今天」快捷按钮
- 分「未完成 / 已完成」两组显示
- 删除需二次确认

---

## 3. 想法

- 纯时间流，最新在前
- 显示相对可读时间（`8月25日 14:30`）
- 删除需二次确认

---

## 4. 训练

### 4.1 两条录入路径

| 优先级 | 路径 | 触发条件 |
|---|---|---|
| **主路径** | 自动同步 | 该日该课型在共享的 `wods` 表里已有 wodify-pull 写入的记录，直接进入选段落/逐段填写/确认 |
| **兜底** | 手写 | 自动同步没有数据（拉取失败/非上课日/想快速记点别的），用训练记录编辑器（见 §4.3） |

> 原来的「粘贴整节课，正则拆段落」录入路径已整体删除，不留兜底——wodify-pull 补完后，
> 手动粘贴纯文本再靠正则猜结构的价值不再存在，双路径徒增维护成本。拉取失败时直接用
> 已有的自由文本训练记录编辑器手写，不走任何"选段落"流程。

### 4.2 自动同步后的三步流程

```
① 选段落 → ② 逐段填写 → ③ 确认并保存
```

**触发条件：该日该课型的 `wods` 已有 wodify-pull 同步的数据**，打开训练 tab 直接进入本
流程，不需要粘贴。**全程内嵌在页面中，不使用浮层。** 浮层在移动端与键盘交互时问题多发
（见 §9.1）。

训练 tab 顶部显示来源提示：有数据时显示「已从 Wodify 同步」+ 同步时间；没有数据时显示
「还没有同步到内容」（不编造原因），引导去用训练记录编辑器（§4.3）手写。已有自动同步
数据时也保留「手动覆盖」入口，可以切到训练记录编辑器重新写。

#### 步骤 ① 选段落

- 列出解析出的所有段落，显示类型、标题、内容预览
- **默认勾选 `strength` 和 `metcon`**，热身/拉伸/附加默认不勾
- 未勾选的段落不进入记录，但**原文始终完整保留**
- 每个段落的类型**可手动修改**（热身/力量/WOD/拉伸/附加）—— 解析出错时的最终兜底

#### 步骤 ② 逐段填写

**力量项（strength）**

- 按计划中的 `Set N: ...` 自动预生成对应组数；无法识别时按 `(N Sets)` 生成，再无则默认 3 组
- 表格四列：`组 | 计划 | 重量 | 次数`
- **「计划」列常驻显示**（如 `6 Reps @ 70%`），与用户实际填写的重量并排

> 用户必须能同时看到「计划是多少」和「我实际做了多少」。

**WOD（metcon）**

- 列出全部 scaling 档位供选择（`RX / Level 2 / Level 1 / Masters 55+ / Competitor / Hotel Gym / Travel` 等）
- 主 WOD 本身作为 `RX` 档，排在最前
- 选中某档位后，**该档位的完整计划常驻显示**，含装备重量（`Barbell: 75/55lb (34/25kg)`）
- 「改动」输入框在计划**下方**，不替换计划

> 加了 modification 之后仍要看得见原本的重量和组数。这是用户明确要求。

- 「成绩」单独一个输入框（`78 reps` / `12:34`）

**所有段落**

- 每段都有「感觉怎么样 / 有什么想法」输入框

#### 步骤 ③ 确认

- 显示**肌群统计**，只统计实际做了的部分：勾选的段落 + 选中的那个档位（选 Level 2 就按 Level 2 的动作算，不按 RX 算）
- 显示自动生成的记录草稿，**用户可自由编辑**
- 可选「用 AI 把它写通顺一点」
- 保存后触发：自动解锁技能（§6.3）、询问约课（§7）

### 4.3 训练记录编辑器

自动同步没有数据时的正式兜底入口，也是"只想快速记一笔"时的常规入口。

- 标题 + 正文（一行一个动作）
- 正文输入时**实时显示解析预览**：识别到几个动作、总容量、每行的重量与组次
- 解析失败不报错，**原文永远原样保留**

### 4.4 历史记录

每条记录三个操作：

| 操作 | 行为 |
|---|---|
| **复制** | 内容填入编辑器，作为**新记录**的起点 |
| 改 | 内容填入编辑器，保存时**覆盖原记录** |
| 删 | 二次确认后删除 |

> **「复制」是最高频操作**，不是「新建」。健身记录的常态是「上次的记录就是这次的模板，改改重量」。

- 卡片显示：标题、动作行、肌群标签、时间、总容量

---

## 5. PR 墙

- 14 个项目：`DL BS FS OHS BP PP PJ J PC C CJ PSN HSN SN`
- 卡片显示：周期表代号、英文名、当前成绩
- 详情：1RM 输入 + `50/60/70/75/80/85/90/95/100/105%` 配重表，100% 高亮
- **KG / LB 可切换**，内部一律存 kg
- 成绩高于原值时提示「破纪录了 🎉」

### 5.1 从训练记录扫描 PR

- 扫描全部训练记录，提取每行重量，匹配动作名
- **中英文别名都要匹配**：`Back Squat` / `后蹲` / `深蹲` / `bs` 都对应同一项
- 找到高于当前记录的，列出并询问是否批量更新
- 没找到更高的：提示「没找到更高的重量」，不弹窗

> 这是自动解析的核心回报：正常记录训练，PR 自己浮出来，不需要二次录入。

---

## 6. 技能树

- 87 个动作，命名依据 CrossFit 官方动作库
- 每个动作有 **2–4 字母代号**（取自 CrossFit 元素周期表，如 `P` `BMU` `HSPU` `TTB`），显示为方块
- 分四类：`FOUNDATIONS / GYMNASTICS / WEIGHTLIFTING / ENDURANCE`
- 搜索支持**名称和代号**（输入 `RMU` 能搜到 Kipping Muscle-up）

### 6.1 无解锁门禁

**任何动作都可以随时标记为已解锁，不检查前置。**

> 不是每个人都按同一顺序练。有人可能先会 muscle-up 才补严格引体。

- `req`（前置动作）**仅作为「相关动作」提示显示**，不做拦截
- 两种状态：已解锁 / 未解锁

### 6.2 心愿单

- 未解锁动作右侧有 **☆**，点亮变 ★ 加入心愿单
- 心愿单置顶显示，标题为「心愿单 · 还差 N 个」
- **点击整行 = 打开解锁确认；只有点 ★ 才移出心愿单**

> 曾经整行点击都是「移出心愿单」，导致用户点哪都是取消。这是明确修复过的 bug。

- 心愿单中的动作被解锁时，自动移出，提示「心愿达成 XXX 🎉」

### 6.3 从训练记录自动解锁

保存训练时扫描记录内容，认出做过的动作并自动解锁。

**记录**：解锁日期、当时重量、**从哪一行认出来的**（便于用户核对误判）、是否自动识别。

**必须有排除词守卫** —— 以下情况不算完成该动作：

- 热身/准备变式：`empty bar`、`banded`、`scapular`、`kip swing`、`build to`、`rehearse`、`prep`、`hold`、`stretch`、`pausing`、`assisted`、`light load`
- 处方建议行：`suggested loading`、`% of`、`1rm`
- 热身专用动作：`cardio choice`、`bootstrap`、`toe touch`、`scorpion`、`arm swing`、`pull-apart`

> 例：Pump 课热身里的 `5 Back Squats (Empty Bar)` 不算做了后蹲。实测热身段落应识别出 **0 个**动作。

**同一行长名优先并占位**：`Back Rack Reverse Lunges` 只匹配 `lunge_brl`，不能同时匹配 `lunge_walk`。
但 `Burpee Pull-Ups` 应同时匹配 `burpee` 和 `pullup_k`（两个独立动作组合）。

---

## 7. 约课提醒

**不自动每周排。** 由记录训练触发：

1. 保存训练后询问「要约下周的课吗」
2. 显示下周同一星期几的日期与时间
3. 上课时间从 wodify-pull 的排课数据（`schedule` 动作）取得；拿不到则让用户手填一次
4. 选「要」→ 创建待办，标题如 `约 周二 5:30 PM 的CrossFit`，**due 定在课前一天**
5. **完成后不自动续排**，勾掉即结束

> 曾经是「每周日自动生成，勾掉自动排下一周」。用户改为按需触发。

---

## 8. 我的

### 8.1 结构

```
圆形头像 + 用户名 + 状态
─────────────────────
小工具   配重计算 / 训练计时器
数据     6 项统计
设置     重量单位 / AI 配置
─────────────────────
导出 JSON / 导入 JSON / 清空数据
```

### 8.2 配重计算

- 输入 1RM → 显示各百分比配重
- **可从 PR 墙一键带入**（显示为 `BS 150` 形式的标签，点击填入）
- KG / LB 可切换

### 8.3 训练计时器

**参数**：回合数、运动时长、休息时长。显示总时长。

**两种修改方式**：

| 方式 | 说明 |
|---|---|
| 加减按钮 | 回合 ±1，时间 ±5 秒 |
| **直接输入** | 点击数字即可编辑 |

**时间输入格式**（两种都要支持）：

| 输入 | 结果 | 规则 |
|---|---|---|
| `1:30` | 90 秒 | 含冒号 → 分:秒 |
| `90` | 90 秒（显示 `01:30`） | **纯数字按秒解析** |
| `45` | 45 秒 | |

**边界**：回合 1–60；运动 5–3600 秒；休息 0–3600 秒（**允许 0**，即无间歇）。
非法输入（`abc`、空）**保持原值不变**，不报错、不清零。

**交互细节**：
- 点击输入框**自动全选**，直接输入新值不必先删
- 按 Enter 收起键盘并生效
- **正在输入的格子不被定时重绘覆盖**（计时器每 100ms 重绘，若无保护会顶掉正在输入的内容）

**运行行为**：
- 阶段：准备 → 运动 → 休息 → …… → 完成
- 运动阶段大色块变实心主色
- 每次阶段切换有短提示音，全部完成有长提示音
- **运行中锁定参数区**（降低不透明度 + 禁止点击），防误触
- 支持暂停 / 继续 / 重置
- **按结束时间戳计算剩余，不做累加**。掉帧或切后台返回后不会走偏

---

## 8.4 空结果不得编造原因

拿不到数据、结果为空时，**只陈述事实，不推测原因**。

反例：参与者列表返回空，界面显示「Wodify 对匿名会员隐藏姓名」—— 这是给一个代码 bug 编造的解释，会让排查方向彻底跑偏。

正确做法：显示「未返回数据」。不知道原因就说不知道。

同样适用于 AI 生成的文案：不得为缺失的数据补充解释。

---

## 9. 已知陷阱（重构时不要重犯）

### 9.1 移动端弹层

1. **不要用 `inset: 0` 配合 JS 设 `height`。** `top`+`bottom` 同时存在时 `height` 被忽略，弹层会铺满「布局视口」而非可见区域，底部对齐的内容被顶到屏幕外。
2. **弹层容器类名必须独一无二。** 曾用 `.box`，与待办复选框的 `.box` 撞车，继承到 `height:18px` 和 `color:#fff`，弹窗被压成一条且文字变白。
3. **不要给弹层内容设 `max-height` 做内部裁切。** 内容过长时应由遮罩层整体滚动，弹层保持自然高度。
4. 弹层内容**屏幕居中**，不用底部对齐 —— 底部对齐依赖安全区、工具栏高度、键盘状态，移动端易碎。
5. 打开弹层时**锁定背景滚动**，关闭时恢复原滚动位置。

### 9.2 其他

- **原生 `<input type="date">` 的一周起始日改不了**，需要周一开始必须自己实现。
- **不要用 `scrollIntoView({block:'center'})` 做打开动画**，会产生突兀的跳动感。用户明确反对。
- **定时重绘不能覆盖正在输入的表单元素**（计时器、任何有输入框的实时刷新界面）。

---

## 10. 变更记录

新增或修改行为时在此追加，注明日期与原因。

| 日期 | 变更 | 原因 |
|---|---|---|
| — | 技能树取消前置解锁门禁 | 用户练习顺序因人而异 |
| — | 约课改为记录训练后触发，不自动续排 | 自动生成的提醒是噪音 |
| — | 计时器时间支持直接输入 | 只能加减太慢 |
| — | 日历改周一开始，加周/月视图，今天与选中分开表示 | 容易忘记今天是哪天 |
| — | 心愿单点行改为解锁，仅 ★ 移出 | 原实现点哪都是取消 |
| 2026-08-26 | 训练记录粘贴解析路径整体删除，改为自动同步（主）+ 手写（兜底） | wodify-pull 补完后正则解析粘贴文本的价值不再存在，双路径徒增维护成本 |
| 2026-08-26 | wodify-pull 密钥架构改为经 Worker 中转，常开机器不再直连 Supabase | 缩小 service_role key 的暴露面 |
